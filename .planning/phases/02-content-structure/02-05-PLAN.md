---
phase: 02-content-structure
plan: 05
type: execute
wave: 5
depends_on:
  - 02-04-PLAN.md
files_modified:
  - style.css
  - script.js
  - index.html
autonomous: true
requirements:
  - HERO-01
  - EXP-01
must_haves:
  truths:
    - "Page scrolls smoothly to sections when nav links are clicked"
    - "Active nav link is highlighted with accent color and underline when its section is in view"
    - "No horizontal overflow at any viewport width"
    - "Experience header columns stack vertically at 600px or narrower"
    - ".sr-only utility class is available for future accessibility use"
  artifacts:
    - path: "style.css"
      provides: "CSS sections 11 (utilities) and 12 (overflow guard media query) filled — all 12 sections complete"
      contains: ".sr-only"
    - path: "script.js"
      provides: "IntersectionObserver active nav highlighting — progressive enhancement"
      contains: "IntersectionObserver"
    - path: "index.html"
      provides: "script.js deferred script tag added before </body>"
      contains: "script.js"
  key_links:
    - from: "script.js IntersectionObserver"
      to: "nav a[href='#experience'] etc."
      via: ".active class toggle"
      pattern: "classList.add\\('active'\\)"
    - from: "html scroll-behavior: smooth"
      to: "nav anchor links"
      via: "CSS scroll-behavior property"
      pattern: "scroll-behavior: smooth"
---

<objective>
Fill the final two CSS sections (utilities and overflow guard), create script.js with the IntersectionObserver active-nav behavior, and add the deferred script tag to index.html. After this plan, all 12 CSS sections are complete and the nav active state is progressively enhanced with vanilla JS.

Purpose: Closes out Phase 2. The utility and media query stubs must be filled to complete the CSS file. script.js is strictly progressive enhancement — the page is fully functional without it, but active nav highlighting adds polish.

Output: style.css sections 11-12 filled (all 12 sections now complete); script.js created at repo root; index.html has deferred script tag.
</objective>

<execution_context>
@/Users/bolotbekbolotov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bolotbekbolotov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/bolotbekbolotov/Documents/newdoc/.planning/ROADMAP.md
@/Users/bolotbekbolotov/Documents/newdoc/.planning/phases/02-content-structure/02-UI-SPEC.md
@/Users/bolotbekbolotov/Documents/newdoc/.planning/phases/02-content-structure/02-CONTEXT.md
@/Users/bolotbekbolotov/Documents/newdoc/.planning/phases/02-content-structure/02-PATTERNS.md

<interfaces>
From style.css (current state after Plan 04):
  - All sections 1-10 are filled
  - Section 11 (UTILITIES): still `/* Phase 2 fills this section */`
  - Section 12 (MEDIA QUERIES): still `/* Phase 2 fills this section */`

From index.html (current state after Plan 04):
  - All sections have real content
  - No <script> tag yet
  - </body> is on the last line before </html>

script.js does not exist yet — create at /Users/bolotbekbolotov/Documents/newdoc/script.js

JavaScript contract (from 02-UI-SPEC.md and 02-PATTERNS.md):
  - IIFE wrapper (function() { 'use strict'; }())
  - No ES6+ syntax (no const/let, no arrow functions) — max browser compat
  - IntersectionObserver with threshold: 0.2
  - Sections to observe: experience, skills, achievements, contact
  - On intersect: remove .active from all nav links, add .active to matching link
  - Feature-detect IntersectionObserver — return early if absent
  - filename: script.js (lowercase)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill style.css sections 11 (utilities) and 12 (overflow guard)</name>
  <files>/Users/bolotbekbolotov/Documents/newdoc/style.css</files>
  <read_first>
    /Users/bolotbekbolotov/Documents/newdoc/style.css (read full file — verify sections 1-10 are intact, confirm only sections 11 and 12 still have placeholder comments)
    /Users/bolotbekbolotov/Documents/newdoc/.planning/phases/02-content-structure/02-UI-SPEC.md (Section 11 UTILITIES contract; Section 12 MEDIA QUERIES contract)
  </read_first>
  <action>
Replace the `/* Phase 2 fills this section */` placeholder after the `/* 11. UTILITIES */` banner with:

```css
.sr-only {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  white-space: nowrap;
  border: 0;
}

html {
  scroll-behavior: smooth;
}
```

Replace the `/* Phase 2 fills this section */` placeholder after the `/* 12. MEDIA QUERIES */` banner with:

```css
/* Phase 2: overflow guard only. Full responsive layout in Phase 3. */
@media (max-width: 600px) {
  .container {
    padding-inline: var(--space-4);
  }

  .experience-header {
    flex-direction: column;
    align-items: flex-start;
  }

  nav {
    gap: var(--space-4);
    flex-wrap: wrap;
  }
}
```

After these two replacements, all 12 CSS sections are filled. The file should have ZERO `/* Phase 2 fills this section */` placeholder comments remaining.
  </action>
  <verify>
    <automated>grep -c '\.sr-only' /Users/bolotbekbolotov/Documents/newdoc/style.css && grep -c 'scroll-behavior: smooth' /Users/bolotbekbolotov/Documents/newdoc/style.css && grep -c 'max-width: 600px' /Users/bolotbekbolotov/Documents/newdoc/style.css && grep -v '^$' /Users/bolotbekbolotov/Documents/newdoc/style.css | grep -c 'Phase 2 fills this section'</automated>
  </verify>
  <acceptance_criteria>
    - `grep '.sr-only' style.css` finds at least 1 match containing `position: absolute`
    - `grep 'scroll-behavior: smooth' style.css` finds exactly 1 match (in html rule in section 11)
    - `grep 'max-width: 600px' style.css` finds exactly 1 match (in section 12 media query)
    - `grep 'flex-direction: column' style.css` finds at least 1 match (experience-header override in media query)
    - `grep 'Phase 2 fills this section' style.css` finds 0 matches — all 12 sections are now complete
  </acceptance_criteria>
  <done>style.css sections 11 and 12 filled. All 12 CSS sections now have real rules. Zero placeholder comments remain in style.css.</done>
</task>

<task type="auto">
  <name>Task 2: Create script.js and add deferred script tag to index.html</name>
  <files>/Users/bolotbekbolotov/Documents/newdoc/script.js, /Users/bolotbekbolotov/Documents/newdoc/index.html</files>
  <read_first>
    /Users/bolotbekbolotov/Documents/newdoc/.planning/phases/02-content-structure/02-PATTERNS.md (script.js full file pattern — exact code with IIFE, IntersectionObserver, feature detection, no ES6+)
    /Users/bolotbekbolotov/Documents/newdoc/index.html (read full file — find the closing </body> tag to add script tag before it)
    /Users/bolotbekbolotov/Documents/newdoc/.planning/phases/02-content-structure/02-UI-SPEC.md (JavaScript Contract section — threshold 0.2, sections to observe)
  </read_first>
  <action>
Create `/Users/bolotbekbolotov/Documents/newdoc/script.js` with the following exact content:

```javascript
(function () {
  'use strict';

  var sections = ['experience', 'skills', 'achievements', 'contact'];
  var navLinks = {};

  sections.forEach(function (id) {
    var link = document.querySelector('nav a[href="#' + id + '"]');
    if (link) navLinks[id] = link;
  });

  if (!('IntersectionObserver' in window)) return;

  var observer = new IntersectionObserver(function (entries) {
    entries.forEach(function (entry) {
      var id = entry.target.id;
      if (navLinks[id]) {
        if (entry.isIntersecting) {
          Object.values(navLinks).forEach(function (l) { l.classList.remove('active'); });
          navLinks[id].classList.add('active');
        }
      }
    });
  }, { threshold: 0.2 });

  sections.forEach(function (id) {
    var el = document.getElementById(id);
    if (el) observer.observe(el);
  });
}());
```

Then edit `/Users/bolotbekbolotov/Documents/newdoc/index.html`: add `<script defer src="script.js"></script>` on the line immediately before `</body>`. The result should be:

```html
  <script defer src="script.js"></script>
</body>
</html>
```

Constraints:
- `defer` attribute is REQUIRED — ensures the DOM is fully parsed before the script runs
- `src="script.js"` — lowercase, relative path, no leading slash (matches GitHub Pages case-sensitive path rules)
- No `type="module"` — the script uses an IIFE, not ES modules
- No ES6+ syntax in script.js (no const/let/arrow functions) — maximum browser compatibility with zero transpile step
- No inline `<script>` blocks — the code is in the external file only
- `'use strict'` inside the IIFE — not at top level (which would affect global scope)
  </action>
  <verify>
    <automated>test -f /Users/bolotbekbolotov/Documents/newdoc/script.js && grep -c 'IntersectionObserver' /Users/bolotbekbolotov/Documents/newdoc/script.js && grep -c 'classList.add' /Users/bolotbekbolotov/Documents/newdoc/script.js && grep -c 'script defer' /Users/bolotbekbolotov/Documents/newdoc/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `test -f script.js` exits with status 0 (file exists)
    - `grep 'IntersectionObserver' script.js` finds at least 2 matches (feature detect + constructor)
    - `grep 'threshold: 0.2' script.js` finds exactly 1 match
    - `grep "classList.add('active')" script.js` finds exactly 1 match
    - `grep "classList.remove('active')" script.js` finds exactly 1 match
    - `grep 'const\|let\|=>' script.js` finds 0 matches (no ES6+ syntax)
    - `grep 'script defer' index.html` finds exactly 1 match
    - `grep 'src="script.js"' index.html` finds exactly 1 match
    - `grep 'http://' script.js` finds 0 matches (no non-secure references)
  </acceptance_criteria>
  <done>script.js exists at repo root with IntersectionObserver active nav behavior. index.html has deferred script tag before </body>. No ES6+ syntax used. Feature detection prevents errors on old browsers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser JS execution | script.js runs client-side — reads DOM, no external requests, no user input |
| script tag in HTML | Deferred script loads from same origin (GitHub Pages) — no external CDN dependency |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Elevation of Privilege | script.js global scope pollution | mitigate | Script is wrapped in IIFE `(function() { 'use strict'; }())` — no variables leak to global scope. `'use strict'` prevents accidental global variable creation. Verified: script has opening `(function ()` and closing `}());`. |
| T-05-02 | Tampering | IntersectionObserver without feature detection | mitigate | Script checks `if (!('IntersectionObserver' in window)) return;` before instantiating — gracefully degrades on old browsers (IE, old Safari). Page is fully functional without JS; this is progressive enhancement only. |
| T-05-03 | Information Disclosure | script.js loading over http:// | mitigate | `src="script.js"` is a relative URL — always served from the same origin as the page (GitHub Pages HTTPS). No CDN or external domain. Verified: `grep 'http://' script.js` returns 0. |
| T-05-04 | Denial of Service | Inline event handlers added to nav links | mitigate | script.js only manipulates class names on existing DOM elements — no new event listeners on links, no onclick handlers, no navigation interference. The only DOM mutation is `classList.add/remove('active')`. |
</threat_model>

<verification>
After both tasks complete:

1. `grep 'Phase 2 fills this section' /Users/bolotbekbolotov/Documents/newdoc/style.css` — finds 0 matches (all 12 sections complete)
2. `grep 'scroll-behavior: smooth' /Users/bolotbekbolotov/Documents/newdoc/style.css` — finds 1 match
3. `test -f /Users/bolotbekbolotov/Documents/newdoc/script.js` — exits 0
4. `grep 'IntersectionObserver' /Users/bolotbekbolotov/Documents/newdoc/script.js` — finds at least 2 matches
5. `grep 'script defer' /Users/bolotbekbolotov/Documents/newdoc/index.html` — finds 1 match
6. `grep 'const\|let\|=>' /Users/bolotbekbolotov/Documents/newdoc/script.js` — finds 0 matches
7. Open index.html in browser, scroll through sections — nav links highlight with accent color as sections scroll into view; clicking nav links smooth-scrolls to section
8. Full page check: `grep 'Phase 2 fills this section' /Users/bolotbekbolotov/Documents/newdoc/style.css` — 0 matches confirms Phase 2 CSS work is complete
</verification>

<success_criteria>
- style.css has ZERO `/* Phase 2 fills this section */` placeholder comments — all 12 sections complete
- html has scroll-behavior: smooth in section 11
- Media query in section 12 provides overflow guard at 600px
- script.js exists with IntersectionObserver, IIFE wrapper, feature detection, no ES6+
- index.html has deferred script tag immediately before </body>
- Nav highlights active section on scroll (progressive enhancement — page works without JS)
</success_criteria>

<output>
After completion, create `/Users/bolotbekbolotov/Documents/newdoc/.planning/phases/02-content-structure/02-05-SUMMARY.md` with:
- Files created (script.js)
- Files modified (style.css: sections 11-12; index.html: deferred script tag)
- CSS sections 11 and 12 filled — all 12 sections now complete
- JS behavior: IntersectionObserver active nav, threshold 0.2, 4 sections observed
- Phase 2 complete: all CV sections authored and rendered
- Status: COMPLETE
</output>
