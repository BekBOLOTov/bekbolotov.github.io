---
phase: 02-content-structure
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01-PLAN.md
files_modified:
  - index.html
  - style.css
autonomous: true
requirements:
  - HERO-01
  - HERO-02
  - HERO-03
  - CONTACT-01
must_haves:
  truths:
    - "Visitor sees full name 'Bolot Bekbolotov' prominently above the fold"
    - "Hero subtitle reads 'Mobile Developer' (not 'Software Engineer')"
    - "Hero summary is 3 sentences with domain, scale, and IOI signal"
    - "Three hero contact links are visible above the fold: email, GitHub, LinkedIn"
    - "Contact section has email, GitHub, and LinkedIn as labeled links"
    - "Footer renders correctly with name and muted styling"
  artifacts:
    - path: "index.html"
      provides: "Filled hero and contact sections with real content and .container wrappers"
      contains: "hero-links"
    - path: "style.css"
      provides: "CSS sections 6 (hero) and 10 (contact/footer) filled"
      contains: ".hero-title"
  key_links:
    - from: "hero-links a[href='mailto:...']"
      to: "email client"
      via: "mailto: href"
      pattern: "href=\"mailto:bekbolotov.bolot@gmail.com\""
    - from: "hero-links a + contact-list a"
      to: "external profiles"
      via: "https:// href with rel=noopener"
      pattern: "rel=\"noopener\""
---

<objective>
Fill the hero and contact sections with real content (HTML + CSS). After this plan, above-the-fold credibility is established: visitor sees the engineer's name, title, 3-sentence specific summary, and contact links immediately on page load.

Purpose: Hero sets first impression. Contact closes the action loop. Both sections are completed in one wave because they share no file conflicts with each other and together fulfill the above-the-fold test.

Output: #hero section with real name/title/summary/links; #contact section with email/GitHub/LinkedIn; CSS sections 6 and 10 filled; footer styled.
</objective>

<execution_context>
@/Users/bolotbekbolotov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bolotbekbolotov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/bolotbekbolotov/Documents/newdoc/.planning/ROADMAP.md
@/Users/bolotbekbolotov/Documents/newdoc/.planning/phases/02-content-structure/02-UI-SPEC.md
@/Users/bolotbekbolotov/Documents/newdoc/.planning/phases/02-content-structure/02-CONTEXT.md
@/Users/bolotbekbolotov/Documents/newdoc/.planning/phases/02-content-structure/02-PATTERNS.md

<interfaces>
From index.html (current state after Plan 01):
  - title is now: Bolot Bekbolotov — Mobile Developer
  - nav has four anchor links
  - section#hero still has: h1 "Bolot Bekbolotov", p.hero-title "Software Engineer", p.hero-summary "Professional summary — Phase 2." — NO .container wrapper
  - section#contact still has: h2 "Contact", placeholder p — NO .container wrapper
  - footer: <p>Bolot Bekbolotov</p> — keep as-is

From style.css (current state after Plan 01):
  - Sections 1-5 filled
  - Section 6 (HERO): still placeholder comment
  - Section 10 (CONTACT / FOOTER): still placeholder comment
  - Sections 7-9, 11-12: still placeholder comments

Hero copy (D-04, verbatim):
  h1: "Bolot Bekbolotov"
  p.hero-title: "Mobile Developer"
  p.hero-summary: "Mobile developer with 7 years of Android and Flutter experience. Built apps used by over 1 million users across e-payment, retail, and logistics platforms. IOI 2016/2017 participant and ACM ICPC NEERC 2018 finalist."
  Links: mailto:bekbolotov.bolot@gmail.com | https://github.com/BekBOLOTov | https://www.linkedin.com/in/bolotbekbolotov/
</interfaces>
</context>

## Phase Goal

**As a** recruiter visiting the page, **I want to** immediately see the engineer's identity, specific professional summary, and contact links above the fold, **so that** I can assess fit in the first 5 seconds without scrolling.

<tasks>

<task type="auto">
  <name>Task 1: Fill style.css section 6 (hero) and section 10 (contact/footer)</name>
  <files>/Users/bolotbekbolotov/Documents/newdoc/style.css</files>
  <read_first>
    /Users/bolotbekbolotov/Documents/newdoc/style.css (read full file — verify sections 1-5 are intact, locate placeholder lines for sections 6 and 10)
    /Users/bolotbekbolotov/Documents/newdoc/.planning/phases/02-content-structure/02-UI-SPEC.md (Section 6 HERO CSS contract; Section 10 CONTACT/FOOTER CSS contract)
  </read_first>
  <action>
Replace the `/* Phase 2 fills this section */` placeholder after the `/* 6. HERO */` banner with:

```css
#hero {
  padding-block: var(--space-16);
  border-top: none;
}

.hero-title {
  font-size: var(--font-size-base);
  color: var(--color-text-muted);
  font-weight: 400;
  margin: 0 0 var(--space-4) 0;
}

.hero-summary {
  font-size: var(--font-size-base);
  line-height: var(--line-height);
  max-width: 600px;
  margin: 0 0 var(--space-6) 0;
  color: var(--color-text);
}

.hero-links {
  display: flex;
  flex-wrap: wrap;
  gap: var(--space-4);
  align-items: center;
}

.hero-links a {
  font-size: var(--font-size-sm);
  font-weight: 600;
  color: var(--color-accent);
  text-decoration: none;
}

.hero-links a:hover,
.hero-links a:focus {
  text-decoration: underline;
}
```

Replace the `/* Phase 2 fills this section */` placeholder after the `/* 10. CONTACT / FOOTER */` banner with:

```css
.contact-list {
  list-style: none;
  padding: 0;
  margin: 0 0 var(--space-12) 0;
  display: flex;
  flex-direction: column;
  gap: var(--space-4);
}

.contact-list li {
  display: flex;
  align-items: center;
  gap: var(--space-4);
  font-size: var(--font-size-base);
}

.contact-label {
  font-weight: 600;
  color: var(--color-text);
  min-width: 80px;
}

.contact-list a {
  color: var(--color-accent);
  text-decoration: underline;
}

.contact-list a:hover,
.contact-list a:focus {
  text-decoration: none;
}

footer {
  border-top: 1px solid var(--color-border);
  padding-block: var(--space-8);
  text-align: center;
}

footer p {
  font-size: var(--font-size-sm);
  color: var(--color-text-muted);
  margin: 0;
}
```

Note: `max-width: 600px` on `.hero-summary` is a permitted hardcoded value (readability layout constraint, not a design token). Do NOT modify sections 1-5 or sections 7-9, 11-12.
  </action>
  <verify>
    <automated>grep -c '\.hero-title' /Users/bolotbekbolotov/Documents/newdoc/style.css && grep -c '\.hero-links' /Users/bolotbekbolotov/Documents/newdoc/style.css && grep -c '\.contact-list' /Users/bolotbekbolotov/Documents/newdoc/style.css && grep -c 'footer p' /Users/bolotbekbolotov/Documents/newdoc/style.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep '.hero-title' style.css` finds at least 1 match containing `color: var(--color-text-muted)`
    - `grep '.hero-links' style.css` finds at least 1 match containing `display: flex`
    - `grep '.contact-list' style.css` finds at least 1 match containing `list-style: none`
    - `grep '.contact-label' style.css` finds 1 match containing `min-width: 80px`
    - `grep 'footer p' style.css` finds 1 match containing `var(--color-text-muted)`
    - `grep 'Phase 2 fills this section' style.css` finds exactly 7 matches (sections 7-9, 11-12 still stubs)
    - `grep '#hero' style.css` finds 1 match containing `padding-block: var(--space-16)`
  </acceptance_criteria>
  <done>style.css sections 6 and 10 are filled with hero and contact/footer CSS. 7 remaining stubs are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Fill index.html hero and contact sections with real content</name>
  <files>/Users/bolotbekbolotov/Documents/newdoc/index.html</files>
  <read_first>
    /Users/bolotbekbolotov/Documents/newdoc/index.html (read full file — note current hero placeholder content and contact placeholder content)
    /Users/bolotbekbolotov/Documents/newdoc/.planning/phases/02-content-structure/02-PATTERNS.md (Hero section pattern and Contact section pattern — exact HTML including .container wrappers)
    /Users/bolotbekbolotov/Documents/newdoc/.planning/phases/02-content-structure/02-CONTEXT.md (D-02: no photo; D-03: email/GitHub/LinkedIn only; D-04: exact summary copy)
  </read_first>
  <action>
Replace the entire `<section id="hero" aria-label="Introduction">` block with the following (adds .container wrapper and real content — the Phase 1 scaffold had no .container div inside hero):

```html
    <section id="hero" aria-label="Introduction">
      <div class="container">
        <h1>Bolot Bekbolotov</h1>
        <p class="hero-title">Mobile Developer</p>
        <p class="hero-summary">Mobile developer with 7 years of Android and Flutter experience. Built apps used by over 1 million users across e-payment, retail, and logistics platforms. IOI 2016/2017 participant and ACM ICPC NEERC 2018 finalist.</p>
        <div class="hero-links">
          <a href="mailto:bekbolotov.bolot@gmail.com">bekbolotov.bolot@gmail.com</a>
          <a href="https://github.com/BekBOLOTov" target="_blank" rel="noopener">GitHub</a>
          <a href="https://www.linkedin.com/in/bolotbekbolotov/" target="_blank" rel="noopener">LinkedIn</a>
        </div>
      </div>
    </section>
```

Replace the entire `<section id="contact" aria-labelledby="contact-heading">` block with:

```html
    <section id="contact" aria-labelledby="contact-heading">
      <div class="container">
        <h2 id="contact-heading">Contact</h2>
        <ul class="contact-list">
          <li>
            <span class="contact-label">Email</span>
            <a href="mailto:bekbolotov.bolot@gmail.com">bekbolotov.bolot@gmail.com</a>
          </li>
          <li>
            <span class="contact-label">GitHub</span>
            <a href="https://github.com/BekBOLOTov" target="_blank" rel="noopener">github.com/BekBOLOTov</a>
          </li>
          <li>
            <span class="contact-label">LinkedIn</span>
            <a href="https://www.linkedin.com/in/bolotbekbolotov/" target="_blank" rel="noopener">linkedin.com/in/bolotbekbolotov</a>
          </li>
        </ul>
      </div>
    </section>
```

Security rules (MANDATORY — every rule must be verified):
- External links (github.com, linkedin.com) MUST have `target="_blank" rel="noopener"`
- All external URLs MUST use `https://` — NEVER `http://`
- The `mailto:` link does NOT use `target="_blank"`
- No inline event handlers (onclick, onmouseover, etc.) anywhere

Keep the `<footer>` unchanged: `<footer><p>Bolot Bekbolotov</p></footer>`
Keep `#experience`, `#skills`, `#achievements` unchanged — they still have placeholder content from Phase 1.
  </action>
  <verify>
    <automated>grep -c 'hero-links' /Users/bolotbekbolotov/Documents/newdoc/index.html && grep -c 'contact-list' /Users/bolotbekbolotov/Documents/newdoc/index.html && grep -c 'rel="noopener"' /Users/bolotbekbolotov/Documents/newdoc/index.html && grep -c 'IOI 2016/2017' /Users/bolotbekbolotov/Documents/newdoc/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'hero-links' index.html` finds at least 1 match (the .hero-links div)
    - `grep 'class="container"' index.html` finds matches inside both #hero and #contact sections
    - `grep 'IOI 2016/2017' index.html` finds exactly 1 match in the hero summary paragraph
    - `grep 'Mobile Developer' index.html` finds at least 2 matches (title tag and p.hero-title)
    - `grep 'rel="noopener"' index.html` finds at least 4 matches (2 hero links + 2 contact links)
    - `grep 'http://' index.html` finds 0 matches (no non-secure URLs)
    - `grep 'contact-list' index.html` finds at least 1 match
    - `grep 'contact-label' index.html` finds exactly 3 matches (Email, GitHub, LinkedIn labels)
    - `grep 'Software Engineer' index.html` finds 0 matches (old placeholder removed from hero)
    - `grep 'Professional summary' index.html` finds 0 matches (placeholder removed)
  </acceptance_criteria>
  <done>index.html hero section has real name/title/summary/links with .container wrapper. Contact section has email/GitHub/LinkedIn with labels. All external links have rel="noopener" and use https://.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → External link targets | Hero and contact sections link to github.com, linkedin.com — opens new tab |
| HTML href values | mailto: and https:// links in static content — no user input involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Elevation of Privilege | External links without rel="noopener" | mitigate | All target="_blank" links MUST have rel="noopener" to prevent reverse tabnapping. Verified in acceptance criteria: `grep 'rel="noopener"' index.html` must return at least 4 matches. |
| T-02-02 | Information Disclosure | http:// links in hero/contact | mitigate | All external hrefs use https:// only. Acceptance criteria: `grep 'http://' index.html` returns 0. This covers the IOI stats link — if it appears here, it must use https://stats.ioinformatics.org (not http://). |
| T-02-03 | Spoofing | Inline event handlers | mitigate | No onclick or other inline JS handlers added. Links use standard href only. No script injection surface in static HTML. |
</threat_model>

<verification>
After both tasks complete:

1. `grep 'hero-links' /Users/bolotbekbolotov/Documents/newdoc/index.html` — finds div.hero-links
2. `grep 'IOI 2016/2017' /Users/bolotbekbolotov/Documents/newdoc/index.html` — finds 1 match in hero summary
3. `grep 'rel="noopener"' /Users/bolotbekbolotov/Documents/newdoc/index.html` — finds at least 4 matches
4. `grep 'http://' /Users/bolotbekbolotov/Documents/newdoc/index.html` — finds 0 matches
5. `grep '.hero-title' /Users/bolotbekbolotov/Documents/newdoc/style.css` — finds rule with color: var(--color-text-muted)
6. `grep '.contact-label' /Users/bolotbekbolotov/Documents/newdoc/style.css` — finds rule with min-width: 80px
7. `grep 'Phase 2 fills this section' /Users/bolotbekbolotov/Documents/newdoc/style.css` — finds 7 matches
8. Open index.html in browser — hero visible above fold with name, "Mobile Developer", 3-sentence summary, email/GitHub/LinkedIn links
</verification>

<success_criteria>
- Hero section: h1 "Bolot Bekbolotov", p.hero-title "Mobile Developer", 3-sentence summary, three links (email/GitHub/LinkedIn) inside .container wrapper
- Contact section: three labeled rows (Email / GitHub / LinkedIn) inside .container wrapper with correct hrefs
- All external links use https:// and have rel="noopener" (per PITFALLS.md Pitfall 8)
- style.css sections 6 and 10 filled; sections 7-9, 11-12 still have placeholder comments
- HERO-01, HERO-02, HERO-03, CONTACT-01 satisfied
</success_criteria>

<output>
After completion, create `/Users/bolotbekbolotov/Documents/newdoc/.planning/phases/02-content-structure/02-02-SUMMARY.md` with:
- Files modified (index.html: hero + contact sections; style.css: sections 6 + 10)
- Hero content added (name, title, summary, 3 links)
- Contact content added (email, GitHub, LinkedIn with labels)
- Requirements satisfied: HERO-01, HERO-02, HERO-03, CONTACT-01
- Status: COMPLETE
</output>
